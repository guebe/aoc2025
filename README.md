All days of advent of code 2025 implemented in Scheme.

Only basic R5RS functionality is used; no additional libraries are required.
As a result, the code is compatible with several Scheme implementations.
It has been tested with Chez, Chicken, and Guile.

The solutions mostly use tail-recursive implementations.
